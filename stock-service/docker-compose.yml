version: '3'
services:
  postgres-db:
    image: postgres:latest
    ports:
      - "5437:5432"
    environment:
      POSTGRES_DB: "pact-cdc"
      POSTGRES_USER: "pact-cdc"
      POSTGRES_PASSWORD: "pact-cdc"
    volumes:
      - postgres-stock:/var/lib/postgresql/data
volumes:
  postgres-stock: